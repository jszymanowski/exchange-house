codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default: false # disable the default status
      backend:
        flags:
          - backend
        paths:
          - "backend/"
      frontend:
        flags:
          - frontend
        paths:
          - "frontend/"

    patch:
      default:
        target: 80%

flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - frontend/

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
